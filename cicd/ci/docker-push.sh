#!/usr/bin/env bash
set -euo pipefail

if [[ $# -lt 2 ]]; then
  echo "Usage: $0 <backend-image-tag> <frontend-image-tag>" >&2
  exit 1
fi

case "$1" in *:git-????????????????????????????????????????) ;; *) echo "Backend tag must use git-<full-sha>." >&2; exit 1;; esac
case "$2" in *:git-????????????????????????????????????????) ;; *) echo "Frontend tag must use git-<full-sha>." >&2; exit 1;; esac

docker push "$1"
docker push "$2"
